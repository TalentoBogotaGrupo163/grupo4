<%= form_with(model: qualification, local: true) do |form| %>
  <% if qualification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(qualification.errors.count, "error") %> prohibited this qualification from being saved:</h2>

      <ul>
      <% qualification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :inscriptions_id %>
    <%= form.text_field :inscriptions_id, id: :qualification_inscriptions_id %>
  </div>

  <div class="field">
    <%= form.label :qualifications_id %>
    <%= form.text_field :qualifications_id, id: :qualification_qualifications_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
