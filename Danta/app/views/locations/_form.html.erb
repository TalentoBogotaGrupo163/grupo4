<%= form_with(model: location, local: true) do |form| %>
  <% if location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(location.errors.count, "error") %> prohibited this location from being saved:</h2>

      <ul>
      <% location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :zone %>
    <%= form.text_field :zone, id: :location_zone %>
  </div>

  <div class="field">
    <%= form.label :neighborhood %>
    <%= form.text_field :neighborhood, id: :location_neighborhood %>
  </div>

  <div class="field">
    <%= form.label :municipality %>
    <%= form.text_field :municipality, id: :location_municipality %>
  </div>

  <div class="field">
    <%= form.label :department %>
    <%= form.text_field :department, id: :location_department %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
